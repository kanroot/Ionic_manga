import { MangaPreviewModel } from './manga-preview.model';

describe('MangaPreviewModel', () => {
  it('should create an instance', () => {
    expect(new MangaPreviewModel()).toBeTruthy();
  });
});
